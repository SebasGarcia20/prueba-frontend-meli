import React from 'react'

/* Se crea el sigueinte componente para mostrar si se digita una ruta no especificada en la app */
export const NotFoundPage = () => {
  return (
    <div>NotFoundPage</div>
  )
}
